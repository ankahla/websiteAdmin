<form class="register-form"  action="{{ path('fos_user_resetting_send_email') }}" method="POST" class="fos_user_resetting_request" style="display:block">
<div class="form-title">
            <span class="form-title">Réinitialiser votre mot de passe</span>
        </div>
        <div class="form-group">
            <input type="text" id="username" name="username" required="required" class="form-control placeholder-no-fix" placeholder="{{ 'resetting.request.username'|trans({}, 'FOSUserBundle') }}" />
        </div>

    <div class="form-actions">
            <button type="submit" id="register-submit-btn" class="btn btn-primary uppercase">{{ 'resetting.request.submit'|trans({}, 'FOSUserBundle') }}</button>
    </div>
</form>